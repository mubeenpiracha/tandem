import React from 'react'
import ReactDOM from 'react-dom/client'

// Application entry point — pages and components to be implemented
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Tandem</div>
  </React.StrictMode>,
)
